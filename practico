var miLista = ["velma", "exploradora", "jane", "john", "harry"];
    for (let i = 0; i < miLista.length; i++) {
        const bucle = miLista[i];
        console.log(bucle); 
    }

i = 0;
while (i < miLista.length);
i++
console.log(miLista[i]);




let saludo = () => "Hola mundo"
console.log(saludo())
